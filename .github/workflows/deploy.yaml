name: Sausage Store Deploy

on:
  push:
    branches:
      - main

env:
  DOCKER_HUB_LOGIN: ${{ secrets.DOCKERHUB_USER }}
  DOCKER_HUB_PASSWORD: ${{ secrets.DOCKERHUB_PASSWORD }}
  NEXUS_HELM_REPO_USER: ${{ secrets.NEXUS_HELM_REPO_USER }}
  NEXUS_HELM_REPO_PASSWORD: ${{ secrets.NEXUS_HELM_REPO_PASSWORD }}
  NEXUS_HELM_REPO_NAME: sausage-store-helm
  NEXUS_HELM_REPO_URL: https://nexus.cloud-services-engineer.education-services.ru/repository/sausage-store-helm
  KUBE_CONFIG: ${{ secrets.KUBE_CONFIG }}
  VAULT_TOKEN: ${{ secrets.VAULT_TOKEN }}

jobs:
  # Сборка и пуш образов в докерхаб:
  build_and_push_to_docker_hub:
      name: Push Docker image to DockerHub
      runs-on: ubuntu-latest
      steps:
        - name: Check out the repo
          # Получение исходного кода из репозитория:
          uses: actions/checkout@v3
        - name: Set up Docker Buildx
          # Установка сборщика контейнеров Docker:
          uses: docker/setup-buildx-action@v3
        - name: Login to Docker 
          # Авторизация на Docker Hub:
          uses: docker/login-action@v3
          # При помощи with передаём в action параметры username и password:
          with:
            username: ${{ secrets.DOCKERHUB_USER }}
            password: ${{ secrets.DOCKERHUB_PASSWORD }}
            # Хорошо ли держать логин и пароль прямо в коде workflow?
            # Нет, это нехорошо. Хранить пароль надо в Secrets.
        - name: Push Backend to DockerHub
          # Одновременный билд и пуш образа в Docker Hub:
          uses: docker/build-push-action@v5
          with:
            # Параметр context: ./backend/ указывает, что нужный Dockerfile 
            # находится в ./backend/
            context: ./backend/
            # Параметр push: true указывает, что образ нужно не только собрать,
            # но и отправить на Docker Hub:
            push: true
            # В параметре tags задаётся название и тег для образа.
            # Для каждого пересобранного образа 
            # устанавливаем тег latest, чтобы потом
            # на сервере и в docker-compose.yml не указывать версию образа:
            tags: ${{ secrets.DOCKERHUB_USER }}/sausage-backend:latest
            cache-from: type=gha
            cache-to: type=gha,mode=max
            build-args: |
              VERSION=${{ github.sha }}
        - name: Push Frontend to DockerHub
          uses: docker/build-push-action@v5
          with:
            context: ./frontend/
            push: true
            tags: ${{ secrets.DOCKERHUB_USER }}/sausage-frontend:latest
            cache-from: type=gha
            cache-to: type=gha,mode=max

        - name: Push Backend-report to DockerHub
          uses: docker/build-push-action@v5
          with:
            context: ./backend-report/
            push: true
            tags: ${{ secrets.DOCKERHUB_USER }}/sausage-backend-report:latest
            cache-from: type=gha
            cache-to: type=gha,mode=max
  # Упаковываем helm-чарт и пушим его в Nexus
  add_helm_chart_to_nexus:
      name: Add Helm Chart To Nexus
      runs-on: ubuntu-latest
      needs: build_and_push_to_docker_hub

      steps:
        - uses: actions/checkout@v3
        - name: Install Helm
          uses: azure/setup-helm@v4
          with:
            version: v3.13.0
        - name: Package Helm chart
          run: helm package ./sausage-store-chart --version 0.1.0
        - name: Add Nexus Helm repo
          run: |
            helm repo add nexus ${{ env.NEXUS_HELM_REPO_URL }} \
              --username ${{ secrets.NEXUS_HELM_REPO_USER }} \
              --password ${{ secrets.NEXUS_HELM_REPO_PASSWORD }}
            helm repo update
        - name: Push chart to Nexus
          run: |
            curl -u ${{ secrets.NEXUS_HELM_REPO_USER }}:${{ secrets.NEXUS_HELM_REPO_PASSWORD }} \
              --upload-file sausage-store-0.1.0.tgz \
              ${{ env.NEXUS_HELM_REPO_URL }}/sausage-store-0.1.0.tgz

  # Деплоим приложения в кластер Kubernetes с помощью helm-чарт
  deploy_helm_chart_to_kubernetes:
      name: Deploy Helm Chart To Kubernetes
      runs-on: ubuntu-latest
      needs: add_helm_chart_to_nexus

      steps:
        - uses: actions/checkout@v3
        - name: Install Helm
          uses: azure/setup-helm@v4
        - name: Setup kubeconfig
          run: echo "${{ secrets.KUBE_CONFIG }}" | base64 -d > kubeconfig
        - name: Add Nexus Helm repo
          run: |
            helm repo add nexus ${{ env.NEXUS_HELM_REPO_URL }} \
              --username ${{ secrets.NEXUS_HELM_REPO_USER }} \
              --password ${{ secrets.NEXUS_HELM_REPO_PASSWORD }}
            helm repo update
        - name: Deploy with Helm
          run: |
            helm upgrade --install sausage-store nexus/sausage-store \
              --namespace sausage-store \
              --create-namespace \
              --set global.vault.host=${{ secrets.VAULT_HOST }} \
              --set global.vault.vaultToken=${{ secrets.VAULT_TOKEN }} \
              --set backend.image=${{ secrets.DOCKERHUB_USER }}/sausage-backend:latest \
              --set backend-report.image=${{ secrets.DOCKERHUB_USER }}/sausage-backend-report:latest \
              --set frontend.image=${{ secrets.DOCKERHUB_USER }}/sausage-frontend:latest \
              --kubeconfig ./kubeconfig